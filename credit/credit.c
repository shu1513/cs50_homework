#include <cs50.h>
#include <stdio.h>

int main(void)
{
   int card_number;
   do
   {
    card_number = get_int("Number: ")
   }
   while (x < 1)

    //booleon valid or not checksum()
    // switch function for card types
    

}