#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    int i = strlen (argv);
    if(argc != 2 || for (j =0; j < i; j++) {argv[j] <58 or argv[j]})
    printf("Usage: ./caesar key\n");

}