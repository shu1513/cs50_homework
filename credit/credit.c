#include <cs50.h>
#include <stdio.h>

int main(void)
{
   int x;
   do
   {
    x = get_int("Number: ")
   }
   while (x < 1)

    //booleon valid or not checksum()
    int checksum;
    ((x % 100)*2 + (x % 10000)*2 + (x % 1000000)*2 + (x % 100000000)*2 +(x % 10000000000)*2 + (x % 1000000000000)*2 + (x % 1000000000000)*2)%10 = 0
    //print invalid,
    //break

    // switch function for card types and card digits
    //print card types if match
    //print invalid if don't match


}