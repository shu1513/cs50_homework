#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    int i = strlen (argv[1]);
    if(argc != 2 || (for (int j =0; j < i; j++) {(int)argv[j] <48 or (int)argv[j]>57}))
    printf("Usage: ./caesar key\n");

}